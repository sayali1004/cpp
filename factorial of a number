#include <stdio.h>
int fact(int n)
{   if(n==0)
      return 1;
    else
    return fact(n-1)*n;
}

int fact1(int n)
{
    int i;
    int F=1;
    for(i=1;i<=n;i++)
      
         F=F*i;
          return F;
      
     
}


int main()
{   int f;
    f=fact(5);
    printf("%d",f);
    return 0;
}
